<%= form_for(@variant_property) do |f| %>
  <% if @variant_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@variant_property.errors.count, "error") %> prohibited this variant_property from being saved:</h2>

      <ul>
      <% @variant_property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :variant_id %><br>
    <%= f.number_field :variant_id %>
  </div>
  <div class="field">
    <%= f.label :property_id %><br>
    <%= f.number_field :property_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
