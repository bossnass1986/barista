- title t('user.address.new.title')
= form_for(@address, :url => myaccount_addresses_path(@address), html: {class: 'custom'}) do |f|
  = render :partial => 'form', :locals => {:f => f}
.columns
  = link_to 'Back to List', myaccount_addresses_path, :class => 'secondary small button  '