- title 'New Merchant'
= form_for @supplier, :url  => admin_suppliers_path do |form|
  = render :partial => '/admin/merchants/form', :locals => {:form => form}
  = submit_tag "Create", :class  => "button"