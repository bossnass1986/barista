- title t('admin.config.tax_rates.index.title')
.mdl-cell.mdl-cell--12-col
  %table.mdl-data-table.mdl-js-data-table.mdl-shadow--2dp.mdl-cell--12-col
    %thead
      %tr
        %th Percentage
        %th
          -#- if Settings.tax_per_state_id
            State
          -#- else
          Country
        %th Start date
        %th End date
        %th Active
        %th
    - @tax_rates.each do |tax_rate|
      %tr
        %td= number_to_percentage(tax_rate.percentage, precision: 2)
        -#- if Settings.tax_per_state_id
        -#  %td= tax_rate.state.name
        -#- else
        %td= tax_rate.country.name
        %td= tax_rate.start_date.to_formatted_s(:default)
        %td= tax_rate.end_date
        %td= tax_rate.active.to_s
        %td.mdl-data-table__cell--non-numeric
          %button.mdl-button.mdl-js-button.mdl-button--icon{:id => tax_rate.id}
            %i.material-icons more_vert
          %ul.mdl-menu.mdl-menu--bottom-left.mdl-js-menu.mdl-js-ripple-effect{:for => tax_rate.id}
            %li.mdl-menu__item= link_to 'Edit Rate',  edit_admin_config_tax_rate_path(tax_rate)
            -#%li.mdl-menu__item= link_to 'Show',  admin_config_tax_rate_path(tax_rate)
            %li.mdl-menu__item= link_to 'Destroy', admin_config_tax_rate_path(tax_rate), :confirm => 'Are you sure?', :method => :delete
.mdl-cell.mdl-cell--12-col
  = link_to 'New Tax rate', new_admin_config_tax_rate_path, :class => 'mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect'