- title 'Find My Coffee'
.grid-block
  .small-12.grid-content
    %section.block-list
      %header I would like a
      %ul
        - @products.each_with_index do |product, i|
          %li{id: i}
            .grid-block
              .small-8.grid-content
                = link_to product_path(product), title: product.name do
                  = product.name.titlecase
                  %strong= number_to_currency product.price_range.first
                  - if product.price_range?
                    to
                    %strong= number_to_currency product.price_range.last
              .small-4.grid-content
                = link_to product_path(product), title: product.name do
                  = image_tag("drinks/#{product.name.titlecase.tr(' ','-')}.png", size: '50', alt: product.name.titlecase, title: product.name.titlecase)

            -#= link_to product_path(product), title: product.name do
              = image_tag("drinks/#{product.name.titlecase.tr(' ','-')}.png", size: '50', alt: product.name.titlecase, title: product.name.titlecase) + product.name.titlecase
              %strong= number_to_currency product.price_range.first
              - if product.price_range?
                to
                %strong= number_to_currency product.price_range.last